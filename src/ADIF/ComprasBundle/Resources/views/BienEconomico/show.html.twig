{% extends '::base_reducido.html.twig' %}

{% block body -%}

    <div class="portlet box blue">

        <div class="portlet-title">
            <div class="caption">
                Detalles del bien econ&oacute;mico: <strong>{{ entity }}</strong>
            </div>
            <div class="tools">
                <a href="javascript:;" class="collapse">
                </a>                
            </div>
        </div>

        <div class="portlet-body">

            <div class="row">                
                <div class="col-md-2">
                    <div class="form-group">
                        <label class="control-label">C&oacute;digo</label>
                        <div class="detalle_show">{{ entity.codigoBienEconomico }}</div>
                    </div>
                </div>                
                <div class="col-md-4">
                    <div class="form-group">
                        <label class="control-label">Denominaci&oacute;n</label>
                        <div class="detalle_show">{{ entity.denominacionBienEconomico }}</div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label class="control-label">Rubro</label>
                        <div class="detalle_show">{{ entity.rubro }}</div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="form-group">
                        <label class="control-label">Tipo</label>
                        <div class="detalle_show">{{ entity.esProducto ? 'Producto' : 'Servicio' }}</div>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-5">
                    <div class="form-group">
                        <label class="control-label">Descripci&oacute;n</label>
                        <div class="detalle_show">{{ entity.descripcionBienEconomico|default('-') }}</div>
                    </div>
                </div>            
                <div class="col-md-5">
                    <div class="form-group">
                        <label class="control-label">Cuenta contable</label>
                        <div class="detalle_show">{{ entity.cuentaContable|default('-') }}</div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="form-group">
                        <label class="control-label">Especificaci&oacute;n t&eacute;cnica</label>
                        <div class="detalle_show">{{ entity.requiereEspecificacionTecnica ? 'Sí' : 'No' }}</div>
                    </div>
                </div>
            </div>

            <div class="form-actions">
                <button type="button" class="btn default button-back" 
                        back-url="{{ path('bieneconomico') }}">
                    <i class="m-icon-swapleft"></i> Volver
                </button>
            </div>
        </div>
    </div>
{% endblock %}
