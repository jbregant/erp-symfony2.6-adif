{
"data": [
{% for entity in clientes %}
    [
    "{{ entity.id }}",    
    "{{ entity.id }}",
    "{{ entity.Provincia }}",
    "{{ entity.Neto|money_format }}",
    "{{ entity.Gravado|money_format }}",
    "{{ entity.Exento|money_format }}"
    ]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}