{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}",
{
"show" : " {{ path( "atributo_show", { "id": entity.id }) }}", 
"edit" : " {{ path( "atributo_edit", { "id": entity.id }) }}",
"delete" : " {{ path( "atributo_delete", { "id": entity.id }) }}"
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
