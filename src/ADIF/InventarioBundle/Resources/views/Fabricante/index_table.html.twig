{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}", 
{
"show" : " {{ path( "fabricante_show", { "id": entity.id }) }}", 
"edit" : " {{ path( "fabricante_edit", { "id": entity.id }) }}",
"delete" : " {{ path( "fabricante_delete", { "id": entity.id }) }}"  
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
