{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}",
{
"show" : " {{ path( "tipomaterial_show", { "id": entity.id }) }}", 
"edit" : " {{ path( "tipomaterial_edit", { "id": entity.id }) }}",
"delete" : " {{ path( "tipomaterial_delete", { "id": entity.id }) }}" 
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
