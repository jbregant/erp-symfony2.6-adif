{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}", 
    "{{ entity.denominacionCorta|default('-') }}",
{
"show" : " {{ path( "tipovia_show", { "id": entity.id }) }}", 
"edit" : " {{ path( "tipovia_edit", { "id": entity.id }) }}",
"delete" : " {{ path( "tipovia_delete", { "id": entity.id }) }}" 
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
