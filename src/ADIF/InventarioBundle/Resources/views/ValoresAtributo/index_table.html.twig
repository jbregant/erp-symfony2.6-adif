{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}",
    "{{ entity.atributo.denominacion|default('-') }}",
{
"show" : " {{ path( "valoresatributo_show", { "id": entity.id }) }}", 
"edit" : " {{ path( "valoresatributo_edit", { "id": entity.id }) }}",
"delete" : " {{ path( "valoresatributo_delete", { "id": entity.id }) }}"
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
