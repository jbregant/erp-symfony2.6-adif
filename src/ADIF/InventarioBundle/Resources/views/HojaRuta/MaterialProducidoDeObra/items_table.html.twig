{
"data": [
	{% for key, item in entity.itemsHojaRutaNuevoProducido %}
[
    "{{ key }}",
    "{{ key }}",

    "{{ item.provincia.nombre|default('-') }}", 
    "{{ item.linea|default('-') }}", 
    "{{ item.almacen|default('-') }}", 
    "{{ item.tipoMaterial|default('-') }}", 
    "{{ item.grupoMaterial|default('-') }}", 
    "{{ item.estadoConservacion|default('-') }}", 

{
    "delete" : "{{ key }}"
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}