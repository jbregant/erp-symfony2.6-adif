{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}",
{
"show" : " {{ path( "tipoactivo_show", { "id": entity.id }) }}",
"edit" : " {{ path( "tipoactivo_edit", { "id": entity.id }) }}",
"delete" : " {{ path( "tipoactivo_delete", { "id": entity.id }) }}"
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
