{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}",
{
"show" : " {{ path( "servicio_show", { "id": entity.id }) }}",
"edit" : " {{ path( "servicio_edit", { "id": entity.id }) }}",
"delete" : " {{ path( "servicio_delete", { "id": entity.id }) }}"
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
