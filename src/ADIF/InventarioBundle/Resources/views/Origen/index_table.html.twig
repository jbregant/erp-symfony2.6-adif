{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}",

{
"show" : " {{ path( "origen_show", { "id": entity.id }) }}", 
"edit" : " {{ path( "origen_edit", { "id": entity.id }) }}",
"delete" : " {{ path( "origen_delete", { "id": entity.id }) }}"  
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
