{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}",
{
"show" : " {{ path( "linea_show", { "id": entity.id }) }}",
"edit" : " {{ path( "linea_edit", { "id": entity.id }) }}",
"delete" : " {{ path( "linea_delete", { "id": entity.id }) }}" 
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
