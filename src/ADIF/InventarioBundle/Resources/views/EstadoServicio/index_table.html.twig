{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}",
{
"show" : " {{ path( "estadoservicio_show", { "id": entity.id }) }}",
"edit" : " {{ path( "estadoservicio_edit", { "id": entity.id }) }}",
"delete" : " {{ path( "estadoservicio_delete", { "id": entity.id }) }}"
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
