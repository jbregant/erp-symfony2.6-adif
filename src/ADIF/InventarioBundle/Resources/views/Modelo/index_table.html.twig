{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}",

{
"show" : " {{ path( "modelo_show", { "id": entity.id }) }}",
"edit" : " {{ path( "modelo_edit", { "id": entity.id }) }}",
"delete" : " {{ path( "modelo_delete", { "id": entity.id }) }}"
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
