{
"data": [
{% for entity in entities %}
    [
    "{{ entity.id }}",
    "{{ entity.id }}",
    "{{ entity.empleado.nroLegajo }}",
    "{{ entity.empleado }}",
    "{{ entity.fechaFormulario|date('d/m/Y') }}",
    {
    "show" : "{{ path( 'formulario572_show', { 'id': entity.id }) }}",
    "edit" : "{{ path( 'formulario572_edit', { 'id': entity.id }) }}"
    }
    ]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}