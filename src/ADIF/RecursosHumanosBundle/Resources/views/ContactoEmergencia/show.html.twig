{% extends '::base.html.twig' %}

{% block body -%}
    <div class="portlet box blue">
        <div class="portlet-title">
            <div class="caption">
                Detalles de la ContactoEmergencia: <strong>{{ entity }}</strong>
            </div>
            <div class="tools">
                <a href="javascript:;" class="collapse">
                </a>                
            </div>
        </div>
        <div class="portlet-body">
            <div class="row">            </div>
                <button type="button" class="btn default button-back" back-url="{{ path('contactos_emergencia') }}"><i class="m-icon-swapleft"></i> Volver</button>
        </div>
{% endblock %}
