{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}", 
    "{{ entity.descripcion|default('-') }}", 
{
"show" : " {{ path( "empleados_nivel_organizacional_show", { "id": entity.id }) }}", 
"edit" : " {{ path( "empleados_nivel_organizacional_edit", { "id": entity.id }) }}" 
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
