{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}", 
{
"show" : " {{ path( "puesto_show", { "id": entity.id }) }}", 
"edit" : " {{ path( "puesto_edit", { "id": entity.id }) }}" 
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
