{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}", 
{
"show" : " {{ path( "tablero_mt_show", { "id": entity.id }) }}", 
"edit" : " {{ path( "tablero_mt_edit", { "id": entity.id }) }}" 
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
