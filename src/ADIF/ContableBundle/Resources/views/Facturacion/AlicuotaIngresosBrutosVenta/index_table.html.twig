{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",    
"{{ entity.descripcion|default('-')|decode }}",    
"{{ entity.valor|default('-') }}", 
{
    "show" : " {{ path( "alicuotasiibb_show", { "id": entity.id }) }}", 
    "edit" : " {{ path( "alicuotasiibb_edit", { "id": entity.id }) }}" 
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
