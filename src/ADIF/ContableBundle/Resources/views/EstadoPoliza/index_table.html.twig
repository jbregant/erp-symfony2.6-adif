{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}",
{
"show" : " {{ path( "estado_poliza_show", { "id": entity.id }) }}",
"edit" : " {{ path( "estado_poliza_edit", { "id": entity.id }) }}"
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
