{
"data": [
{% for entity in entities %}
    [
    "{{ entity.id_cuenta_contable }}",
    "{{ entity.id_asiento_contable }}",
    "{{ entity.codigo_cuenta_contable|escape_json }}",
    "{{ entity.denominacion_cuenta_contable|escape_json }}",
    "{{ entity.denominacion_asiento_contable|escape_json }}",
    "{{ entity.importe_mcl|currency_format }}",
    "{{ entity.fecha_contable }}"
    ]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
