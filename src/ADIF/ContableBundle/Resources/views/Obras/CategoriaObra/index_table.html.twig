{
"data": [
{% for entity in entities %}
    [
    "{{ entity.id }}",
    "{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}", 
    {
    "show" : " {{ path( "categoriaobra_show", { "id": entity.id }) }}", 
    "edit" : " {{ path( "categoriaobra_edit", { "id": entity.id }) }}" 
    }
    ]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
