{
"data": [
{% for entity in entities %}
[
"{{ entity.id }}",
"{{ entity.id }}",
    "{{ entity.idComprobante|default('-') }}", 
    "{{ entity.idProveedor|default('-') }}", 
    "{{ entity.idCliente|default('-') }}", 
    "{{ entity.total|default('-') }}", 
    {% if entity.fechaComprobante %}"{{ entity.fechaComprobante|date('d/m/Y') }}",{% endif %} 
    "{{ entity.esNotaCredito|default('-') }}", 
    "{{ entity.observaciones|default('-') }}", 
    "{{ entity.idUsuarioCreacion|default('-') }}", 
    "{{ entity.idUsuarioUltimaModificacion|default('-') }}", 
{
"show" : " {{ path( "comprobante_ajuste_show", { "id": entity.id }) }}", 
"edit" : " {{ path( "comprobante_ajuste_edit", { "id": entity.id }) }}" 
}
]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
