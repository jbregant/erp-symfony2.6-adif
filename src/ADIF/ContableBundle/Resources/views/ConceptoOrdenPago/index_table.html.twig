{
"data": [
{% for entity in entities %}
    [
    "{{ entity.id }}",
    "{{ entity.id }}",
    "{{ entity.denominacion|default('-') }}", 
    "{{ entity.cuentaContable|default('-') }}", 
    {
    "show" : " {{ path( "concepto_orden_pago_show", { "id": entity.id }) }}", 
    "edit" : " {{ path( "concepto_orden_pago_edit", { "id": entity.id }) }}" 
    }
    ]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
