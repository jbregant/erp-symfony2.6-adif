{
"data": [
{% for entity in entities %}
    [
    "{{ entity.id }}",
    "{{ entity.id }}",
    "{{ entity.denominacion|default('-')|decode }}", 
    "{{ entity.descripcion|default('-')|decode }}", 
    {
    "show" : " {{ path( "estadorengloncobranza_show", { "id": entity.id }) }}", 
    "edit" : " {{ path( "estadorengloncobranza_edit", { "id": entity.id }) }}" 
    }
    ]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
