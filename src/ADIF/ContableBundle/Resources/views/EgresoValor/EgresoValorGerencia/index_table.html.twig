{
"data": [
{% for entity in entities %}
    [
    "{{ entity.id }}",
    "{{ entity.id }}",
    "{{ entity.tipoEgresoValor|default('-') }}",
    "{{ entity.gerencia|default('-') }}",
    "{{ entity.monto|money_format }}",  
    "{{ entity.cuentaContable }}",  
    {
    "show" : " {{ path( "egresovalor_egresovalorgerencia_show", { "id": entity.id }) }}",
    "edit" : " {{ path( "egresovalor_egresovalorgerencia_edit", { "id": entity.id }) }}" 
    }
    ]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}
