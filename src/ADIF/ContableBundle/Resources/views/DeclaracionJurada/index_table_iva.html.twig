{
"data": [
{% for entity in entities %}
    [
    "{{ entity.id }}",
    "{{ entity.id }}",
    "{{ entity.tipoImpuesto }}",
    "{{ entity.regimen }}",
    "{{ entity.fecha|date('d/m/Y') }}",
    "{{ entity.nombreBeneficiario }}",
    "{{ entity.monto|money_format }}"
    ]{{ (loop.last ? '' : ',') }}
{% endfor %}
]}